# Build artifacts
/target

# Library: lockfile is regenerated by downstream consumers
/Cargo.lock

# OMC orchestration working state
/.omc
/.omx

# Profiling artifacts (perf record / cargo-flamegraph)
perf.data
perf.data.old
*.perf
flamegraph.svg

# Benchmark workspace build artifacts
/benchmark/target
